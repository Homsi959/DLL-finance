import { IconArrowLight } from 'components/icons';
import palette from 'theme/palette';

export const IconArrowLightMain = () => {
  return <IconArrowLight colors={{ fill: palette.primary.main }} />;
};
