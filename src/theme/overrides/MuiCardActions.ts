import { CardActionsClassKey, StyleRules } from '@material-ui/core';

const style: Partial<StyleRules<CardActionsClassKey>> = {
  root: {
    padding: '8px 16px',
  },
};

export default style;
