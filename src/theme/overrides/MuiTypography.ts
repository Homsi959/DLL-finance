import { TypographyClassKey, StyleRules } from '@material-ui/core';

const style: Partial<StyleRules<TypographyClassKey>> = {
  gutterBottom: {
    marginBottom: 8,
  },
};

export default style;
