import { PageLayout } from 'components';
import { LoginPage as LoginPageContent } from './LoginPage';

export const LoginPage = () => {
  return <PageLayout pageContent={<LoginPageContent />} />;
};
